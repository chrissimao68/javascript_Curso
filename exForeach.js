//exemplo foreach
let games = ["Minecraft", "Fortnite", "Roblox"];
//Mostrar cada game com emoji
games.forEach((game, index) => {
    console.log(`${index + 1}. ${game}`);
});

games.forEach((elemento, indice) => {
    //Ação para cada elemento
});